import cl from "./newQuizPage.module.scss";

const Page = () => {
  return (
    <main style={{ display: "none" }} className={cl.newQuizPageWrapper}>
      Page
    </main>
  );
};

export default Page;
